# 从内容派生文章索引并注入题库提供者

文章索引由目录清单和文章元数据在一次构建中确定性派生，不再使用 LokiJS 作为渲染前置数据库；跨文章链接通过该索引查询。外部题库通过可注入的 `ProblemProvider` 接入，未配置题库时仍允许构建不依赖题库的文章，并对依赖题库的扩展报告明确诊断。这样消除了重复落盘、更新顺序和仓库外硬编码路径带来的隐式状态。

## Status

accepted
